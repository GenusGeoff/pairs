"""Parameters used for backtesting"""

params_all = dict(
    base_daily = dict(
        window_std = 15,
        window_corr = 15,
        factor_std = 2,
        factor_profit_std = 1.5,
        factor_loss_size = 3,
    )
)
